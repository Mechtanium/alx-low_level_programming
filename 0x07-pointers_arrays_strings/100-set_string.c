#include <stdlib.h>
#include <stdio.h>
#include "main.h"

/**
 * set_string - Entry point
 * @s: Double pointer
 * @to: Pointer to replacement string
 *
 * Description: Add question here
 * Return: 0 (success) and 1 (failure) to execute
 */
void set_string(char **s, char *to)
{
	*s = to;
}
